# Goal

## 1. Intent


## 2. Narrative


## 3. Success Model
<!--
  可选 workunit 分组：当一个 Goal 含多个可独立描述的切片（如「认证」「授权」）时，
  在本节内用 `### Workunit: <name>` 子标题分组组织下列字段（Goal Constraints / Scope 同理）。示例：
      ### Workunit: 认证
      - user_visible_success: 用户可用邮箱密码登录
      - must_not_happen: 密码出现在日志
  workunit 仅用于文档归类与 criteria 追溯，不构成实施顺序——实施仍由 Master 按 criterion 驱动，
  不按 workunit 依次执行。简单 Goal 保持下列扁平结构即可，并删除本注释。

  minimum_acceptable_result 与 final_completion_signal 是「验收点」，须从 intake-capture 的
  Acceptance Signals 投影：每条必须是可判定的 done 条件（能描述 fail 长什么样），不是「能力存在」
  （「页面可加载/能展示 X」只证存在、不证成功，算弱验收点，要收紧）。效用/统计类成功（如「回测能否
  支撑投产判断」）若不可机器判定，下游对应 criterion 标 kind: judgment，不强转 machine。
  边界：验收点只防实现期偏离已确认口径，挡不住「设计模型本身错」（那需设计阶段/人类领域审查）。
-->
- user_visible_success:
- system_observable_success:
- must_not_happen:
- minimum_acceptable_result:
- final_completion_signal:

## 4. Scope
- in_scope:
- out_of_scope:

## 5. Risk Scan
- scope-boundary:
- actor-permission:
- data-lifecycle:
- failure-degradation:
- non-functional-baseline:
- integration-boundary:

## 6. Goal Constraints

## 7. Sources and Decisions
- sources:
- confirmed_decisions:
- assumptions:

## 8. Open Questions

## 9. Reopen Triggers
