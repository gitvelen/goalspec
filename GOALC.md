- 在一个空 git repo 中，运行 `.goalspec/goalspec init` 后生成完整 `.goalspec/`、短 `AGENTS.md/CLAUDE.md`，且 `goalspec status` 给出明确 `NEXT_ACTION`。
- 在非 git repo 中运行 `goalspec init` 必须失败，并提示先初始化 git。
- 未通过 intake review 和 goal approval 时，`compile/freeze/next` 被阻断，并输出具体原因。
- `goal.md` 缺 Narrative、Success Model、Scope、Risk Scan 或存在 blocking open question 时，intake review 不能 pass。
- `contract.yaml` 中缺 criteria、criteria 不可判定、goal coverage 缺口、WU 未绑定 criteria、allowed paths 过宽且无 approval 时，不能 freeze。
- `goal.md` 改动后，旧 intake review、goal approval、contract review、contract approval 和 frozen contract 必须被判为 stale；未经重新 review/approve/freeze 不得继续执行。
- `contract.yaml` 改动后，旧 evidence 和旧 verdict 必须 stale；`judge apply` 或 `complete` 不得继续接受旧 hash。
- `memory-patch.yaml` 改动后，旧 memory-patch approval 必须 stale；未重新 approval 不得 complete。
- `freeze` 前业务代码 dirty 必须失败；`.goalspec/active` 内编译相关变化不应被误判为业务 dirty。
- Frozen contract 后，executor 修改 `contract.yaml`、`verdict.yaml`、`project/**`、`history/**` 或 WU forbidden paths，`scope-check` 必须失败。
- `next` 一次只能返回一个 WU；当当前 WU 最新 verdict 为 `fail` 或 `insufficient` 时，`next` 必须继续返回同一 WU，不得跳过。
- 没有 fresh context guardian verdict 时，即使测试命令退出码为 0，`complete` 也必须失败。
- executor 追加的 evidence/trace 即使显示“已完成”或“已通过”，也不能直接让 state 进入 `completed`；只有 guardian verdict 与 `complete` gate 可以完成状态转移。
- Guardian verdict 缺 `context: fresh`、criteria/evidence 引用不存在、hash 不匹配、pass 未引用满足 evidence requirement 的 evidence 时，`judge apply` 必须失败。
- blocking question 存在时，`freeze` 必须失败；execution unknown 必须进入 `blocked` 或 `reopen_required`，不能伪装成 `pass`。
- Required criteria 任一最新 verdict 为 `fail/insufficient/blocked/stale/reopen_required` 时，`complete` 必须失败并给出下一步动作。
- 所有 required criteria、final criteria、hard constraints 最新 verdict 为 `pass`，无 blocking question，scope-check pass，memory-patch 已 human approve 时，`complete` 成功。
- `complete` 成功后必须更新 `project/memory.yaml`、`project/versions.yaml`、`project/regression-suite.yaml`，并归档 active 文件到 `history/vNNNN/`。
- `complete` 前 dirty 可接受，但所有业务 changed files 必须归属到已 pass WU；未归属文件必须阻断 complete。
- Locked regression 在后续相关 contract 中必须注入 required evidence；regression waiver 必须有 human approval。
- `status` 和 `next` 输出必须包含 `STATE`、`NEXT_ACTION`、`ROLE`、`READ`、`MAY_EDIT`、`MUST_NOT_EDIT`、`BLOCKERS`、`CURRENT_WORK_UNIT`、`COMPLETION_CONDITION`。
- human approval 只能用于 `goal`、`contract`、`memory-patch`、`high-risk action`、`regression waiver`；approval 不能把 fail 的 criteria 直接改判为 pass。
- 贪吃蛇示例项目必须能从一句人类输入编译出完整 `goal.md`、行为型 WU、browser 级 criteria/evidence，并证明没有 guardian verdict 时不能以“游戏能玩”完成,最后在贪吃蛇项目目录中输出完成的验收报告（证明是按goalspec框架开发，并且如实记录碰到的问题）。
- smoke 和所有负例测试通过，且 Goalspec 命令输出足够 Claude/Codex 根据角色文件继续执行，无需读取 Codespec 文件。
